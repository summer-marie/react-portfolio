# CLAUDE.md

## Purpose

This file holds Claude Code-specific notes for this repository.

`AGENTS.md` is the canonical shared policy for every agent working in this repo (operating mode, git workflow, commit rules, testing standards, implementation standards, and handoff requirements). Claude Code must read and follow `AGENTS.md` in full before making any changes.

This file may only add constraints on top of `AGENTS.md`. It must never weaken or contradict it.

## Claude-Specific Notes

None yet. Add Claude Code-specific conventions here as they come up (e.g. preferred tool usage, subagent behavior, output style) rather than duplicating anything already covered in `AGENTS.md`.
